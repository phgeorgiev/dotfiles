alias ip="dig +short myip.opendns.com @resolver1.opendns.com"
alias sail='sh $([ -f sail ] && echo sail || echo vendor/bin/sail)'
alias php-fix='g diff --name-only | xargs --no-run-if-empty php-cs-fixer fix'
